# IDV-ASAS 设计摘要

> 《第五人格》区域选择辅助系统 — Design Brief

---

## 项目一句话

面向《第五人格》排位赛求生者阵营，通过大语言模型 + 专属数据库，提供**天赋选择与区域选择的联合推荐方案**的桌面应用。

---

## 背景

《第五人格》是网易 2018 年推出的非对称对抗手游（1 监管者 vs 4 求生者）。Ban/Pick 后进入**区域选择**环节，双方在地图 9～12 个区域中各选出生点，选点优劣直接影响对局走向。官方于 2026 年 7 月上线选点推荐，但存在三大不足，IDV-ASAS 旨在解决这些问题。

---

## 痛点 → 方案

| 痛点 | IDV-ASAS 方案 |
|------|--------------|
| 官方推荐未考虑天赋对选点的影响 | 天赋-选点**联合推荐**：牵制天赋可占危险点，运营天赋放安全点 |
| 官方对每位玩家单独推荐，缺乏队伍联动 | 接入角色联动数据库，联动方放邻近点位；为每人展示**全队**方案 |
| 通用 Agent 不理解游戏机制、版本滞后 | 构建 51 角色 + 9 地图**专属数据库**，注入 system prompt |
| 高手经验散落网络，难以系统化 | 整合选点模式、禁区/必选区约束为模型决策策略 |

---

## 技术栈

| 层面 | 技术 |
|------|------|
| 语言 | Rust 1.75+ |
| GUI | eframe 0.36 + egui 0.36 (1280×720) |
| 渲染 | wgpu 30 (Vulkan + GL, X11 only) |
| HTTP | ureq 3.4 (SSE 流式) |
| 序列化 | serde + serde_json |
| 字体 | 内嵌 Deng.ttf |

单进程前后端一体化，编译为单一可执行文件。

---

## 架构

```
┌──────────── 前端 (UI 层) ────────────┐
│  eframe/egui · 12 个步骤界面        │
│  ImageCache · 选点图合成            │
└──────────────┬──────────────────────┘
               │ 函数调用 / mpsc 通道
┌──────────────┴── 后端 (逻辑层) ─────┐
│  状态机 (11 状态 FSM)               │
│  prompt.rs · api.rs · parser.rs     │
│  config.rs · history.rs · checker   │
└──────────────┬──────────────────────┘
               │ 文件读取
┌──────────────┴── 数据资源层 ───────┐
│  resources/ (角色+地图 .md)         │
│  icons/ (头像+底图+天赋 PNG)        │
│  DICTIONARY.md                      │
└──────────────┬──────────────────────┘
               │ HTTPS
┌──────────────┴── 外部模型 ─────────┐
│  OpenAI 兼容 API (流式 SSE)         │
│  OpenAI / DeepSeek / SiliconFlow    │
└─────────────────────────────────────┘
```

---

## 核心流程

```
配置API → 导入历史? → 全局需求 → 选地图 → 选4角色 → 当局需求
                                                         ↓
         ┌──── 打断/错误 → ErrorExit (重试/退出) ◄────────┤
         ↓                                               │
   调用模型分析 (独立线程, 可打断)                        │
         ↓                                               │
   解析校验 (最多3次重试)                                │
         ↓                                               │
   展示方案 (选点图+天赋图标+分析) ←── 翻页浏览 ──┐      │
         ↓ 确认使用                              │      │
   对局等待 → Token/费用统计                     │      │
         ↓                                      │      │
   下一局? ─是→ 回到选地图 (全局需求保留)────────┘──────┘
         否→ 保存历史? → 退出
```

---

## 核心模块

| 模块 | 职责 |
|------|------|
| `prompt.rs` | 静态资料注入 system prompt；每局动态组装 full/compact request |
| `api.rs` | 独立线程 SSE 流式调用；`Arc<AtomicBool>` 打断；Token 统计/估算 |
| `parser.rs` | 三阶段：方案拆分→逐方案解析→合法性校验（角色/天赋/选点/字数） |
| `images.rs` | PNG 按需解码→TextureHandle 缓存；选点底图+角色头像内存合成 |
| `history.rs` | Session JSON 保存/加载；紧凑请求避免 Token 膨胀；累计统计 |
| `checker.rs` | 启动时扫描 resources/，构建角色与地图索引 |

---

## 数据资源

| 资源 | 规模 | 注入方式 |
|------|------|---------|
| 角色数据 | 51 个 .md（六维+天赋+联动） | 每局动态注入 user prompt |
| 地图数据 | 9 个 .md（选点+板窗+模式） | 每局动态注入 user prompt |
| 通用总纲 | README + characters/maps README | 启动时注入 system prompt |
| 字典 | DICTIONARY.md（中英文映射） | 启动时注入 system prompt |
| 图标 | 51 头像 + 9 底图 + 9 概念图 + 4 天赋 | 按需加载到 ImageCache |
| 坐标 | 9 个 positions/.md | 结果展示时解析选点中心坐标 |

---

## 关键设计决策

1. **天赋-选点联合**：不止推荐选点，还推荐每人天赋搭配，因天赋影响选点策略
2. **线程化调用**：API 在独立线程执行，UI 不阻塞，`ctx.request_repaint()` 驱动重绘
3. **可打断**：`AtomicBool` 共享标记，API 线程 150ms 轮询检测中断
4. **自动重试**：输出格式校验失败自动重试，追加错误说明，最多 3 次
5. **兼容降级**：`stream_options` 不支持时自动降级，Token 按字符数×0.75 估算
6. **紧凑续局**：历史局省略大段资料进入上下文，避免多局后 Token 膨胀

---

> 完整设计详见 [DESIGN-DOC.md](./DESIGN-DOC.md)（1367 行）。
