## 使用流程介绍

你需要实现一个 IDV-ASAS(Identity V Area Selection Assistance System) 项目，其使用流程如下：

第 0 步：配置 API 接口。

如果无默认配置，则让用户依次输入 API-key 等信息并保存在默认配置中。

如果有默认配置，则询问用户是否沿用，否则让用户更新默认配置。

API 的兼容性可参考 ./test/USAGE.md 中的 API 接口部分。

第 1 步：输入全局用户需求。

用户将键入全局的用户需求。这一部分将会在以后每一小局均有使用。

第 2 步：选择当局地图。

用户将从 9 张排位地图中选择当局的地图。

第 3 步：选择当局角色。

用户将从总计 51 名角色中选择 4 名互不相同的角色作为当局角色。

第 4 步：输入当局用户需求。

用户将键入当局的用户需求。这一部分和用户的全局用户需求共同构成了用户需求。

第 5 步：调用模型进行分析。

具体的调用方法将在 ./resources/AGENTS.md 中介绍。

这一过程需要支持打断。

若出现错误情况，可以参考 ./test/USAGE.md 中的实现。

第 6 步：展示模型分析结果。

具体的展示方法将在 ./icons/AGENTS.md 中介绍。

第 7 步：等待用户结束对局并确认。

确认完毕后，为用户报告当局调用所消耗的 Token 用量以及费用。

第 8 步：询问用户是否开启下一对局。

如果否，则退出程序。

如果是，则返回至第 2 步。需要注意的是，全局的用户需求不需要重新键入。

## 功能需求介绍

### 交互界面

具体交互界面所需要的功能将在 ./icons/AGENTS.md 中详细介绍。

### 历史保存与导入

如果有历史信息，则在第 0 步结束后且在第 1 步开始前询问用户是否导入。如果导入，则展示历史列表。否则直接新开。

用户在任何一步退出程序前（例如完成对局不希望开启下一局，又如 API 接口错误导致无法分析用户不希望重试直接退出等），都需要询问用户是否保存历史信息以方便导入。

如果用户选择保留，则询问用户是否命名。如果是，则采用用户命名命名该历史信息。如果否，则以当前时间命名历史信息。

如果用户选择不保留，则舍弃该历史信息。

### 实时展示进度

这是用于第 5 步中向用户展示进度的功能。目前暂时不做要求。可以预留一定的空间等待后续指示。

不过需要注意的是，不管这一功能最后是否实现，支持打断功能都是需要实现的，支持打断功能是独立于实时展示进度的。

## 实现建议与指南

该程序共分为前端和后端两个部分。

前端部分参照 ./icons/AGENTS.md 中的建议进行实现。

后端部分参考 ./resources/AGENTS.md 中的建议实现。

同时，后端部分还可以参照 ./test 中的测试项目实现。可通过阅读 ./test/USAGE.md 与 ./test/README.md 弄明白测试项目的使用方式与实现方法。如果需要的话，后端部分可以复用 ./test/src 中的代码。

## 实现要求与注意事项

尽管我们将程序分为前端与后端，但实质上他们只是该应用的一部分，不需要联网实现服务端与客户端之间的关系。因此，文中所有的“发送”只是该程序内部的消息传递，切忌搞成联网传递。其余的方式例如函数调用、信息通道等均可，不过在保证功能的前提下，避免不必要的阻塞或延迟。

注意：文中所有路径均是以该文件所在地为起始目录的。

需要注意的是，./test 中的实现所用的资源路径原为 IDV-resources，现已改为 resources，复用时请注意替换。